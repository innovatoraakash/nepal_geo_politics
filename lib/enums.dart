///[AreaType] has 4 types where all specifies 3 level of combined administrative level with hierarchi
///other [AreaType] like district, province, localLevel contains all the data of individual itself
///which are unrelated to each other
enum AreaType {
  ///all specifies 3 level of combined administrative level with hierarchi
  all,
  district,
  province,
  localLevel,
}
